apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: kserve-lab
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
